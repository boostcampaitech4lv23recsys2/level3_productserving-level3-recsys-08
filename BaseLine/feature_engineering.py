import pandas as pd

def FE(data : pd.DataFrame) -> pd.DataFrame:
    """Feature Engineering 함수를 추가해 주세요

    Args:
        data (pd.DataFrame): Raw DataFrame

    Returns:
        pd.DataFrame: FE 완료된 DataFrame
    """
    return data